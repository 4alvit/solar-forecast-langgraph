{
  "required": [
    "solar_forecast",
    "pyproject.toml",
    "uv.lock",
    "Dockerfile"
  ],
  "python_distribution": true,
  "containers": {
    "solar-forecast-langgraph-container.oci.tar": {
      "context": ".",
      "dockerfile": "Dockerfile",
      "platforms": "linux/amd64,linux/arm64"
    }
  }
}
